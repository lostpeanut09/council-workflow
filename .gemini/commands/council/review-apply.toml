description = "Apply only HIGH/MEDIUM from docs/REVIEW_KILO.md, add tests, re-run tests."
prompt = """
You MUST:
- Apply ONLY HIGH and MEDIUM items from the review.
- Ignore LOW/nits unless they are 1-line and reduce risk.
- Add/adjust tests for each fix.
- Re-run the project's test command (prefer docs/PLAN.md if present).

Review:
@{docs/REVIEW_KILO.md}

Now implement.
"""
